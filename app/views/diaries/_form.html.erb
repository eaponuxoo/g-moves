<%= form_for(@diary) do |f| %>
  <% if @diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
      <% @diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"タイトル(20字以内)" %><br>
    <%= f.text_field :title, :size=>"40", :maxlength=>"20" %>
  </div>
  <div class="field">
    <%= f.label :日付 %><br>
    <%= f.number_field :date %>
  </div>
  <div class="field">
    <%= f.label :本文 %><br>
    <%= f.text_field :content, :class =>"inputcontent" %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
